# تشغيل التطبيق

## الخطوات:

### 1. تشغيل الخادم (Backend)
```bash
npm run start
```
الخادم سيعمل على المنفذ: `http://localhost:3001`

### 2. تشغيل الواجهة (Frontend)
في نافذة terminal جديدة:
```bash
npm run dev
```
الواجهة ستعمل على المنفذ: `http://localhost:5173`

## ملاحظات:
- تأكد من تشغيل الخادم أولاً قبل الواجهة
- تأكد من إعداد قاعدة البيانات في ملف `.env`
- القيود اليومية الآن متصلة بقاعدة البيانات بشكل كامل
